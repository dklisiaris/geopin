<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".BaseActivity">
    <item android:id="@+id/action_settings" android:title="@string/action_settings"
        android:orderInCategory="100" app:showAsAction="never" />

    <item android:id="@+id/addPin"
        android:icon="@drawable/ic_action_add_pin"
        android:title="@string/add_pin"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/showHide"
        android:icon="@drawable/ic_action_action_visibility"
        android:title="@string/hide_pins"
        app:showAsAction="ifRoom|withText"/>
</menu>
